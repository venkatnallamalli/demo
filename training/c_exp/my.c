#include<stdio.h>
#include<stdlib.h>
int main()
{
	int a = 10;
/*	char *str;
	FILE *f;
	str = (char*)malloc(128);
	if(NULL == (f = fopen(__FILE__,"r"))){
		perror("File open:");
		exit(EXIT_SUCCESS);
	}
	while(((fgets(str, 128, f)) != NULL) && printf("%s",(str))); 
	
	//char c;
	//while((c = fgetc(f)) != EOF)
	//		printf("%c",c);*/
	return 0;
}
