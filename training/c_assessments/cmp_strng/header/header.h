//prototypes

#include<stdio.h>
#include<string.h>
#include<stdlib.h>

#define MAX 100			//MAximum no.of characters to read from the user

int str_cmp(char*, char*);		//used for comparing two strings

void to_lower(char*);			//to convert the string in to lower case

int valid(char*);		//to check the given input is wrong otr not
